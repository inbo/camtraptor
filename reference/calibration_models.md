# Sample of deployment calibration models

A list containing a number of calibration models (`calibs`) or site
calibration models (`depcal`). The deployment names are used as names.

## Usage

``` r
calibration_models
```

## Format

An object of class `calibs` of length 4.

## See also

Other sample data:
[`animal_pos`](https://inbo.github.io/camtraptor/reference/animal_pos.md),
[`animal_positions`](https://inbo.github.io/camtraptor/reference/animal_positions.md),
[`calib_models`](https://inbo.github.io/camtraptor/reference/calib_models.md),
[`mica`](https://inbo.github.io/camtraptor/reference/mica.md)
