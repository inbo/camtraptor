# Features returned by `summarize_deployments()`. Useful to check output of the function and other check functions.

Features returned by
[`summarize_deployments()`](https://inbo.github.io/camtraptor/reference/summarize_deployments.md).
Useful to check output of the function and other check functions.

## Usage

``` r
.features_deployments
```
