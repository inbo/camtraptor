# Features returned by `summarize_observations()`. Useful to check output of the function and other check functions.

Features returned by
[`summarize_observations()`](https://inbo.github.io/camtraptor/reference/summarize_observations.md).
Useful to check output of the function and other check functions.

## Usage

``` r
.features_observations
```
