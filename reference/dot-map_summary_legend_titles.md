# Map legend title table

Store legend titles for deployment visualizations: RAI, effort, number
of observations, etc. A data frame of all titles with the following
columns:

- `feature`: Deployment feature to visualize.

- `legend_title`: Legend title.

## Usage

``` r
.map_summary_legend_titles
```
